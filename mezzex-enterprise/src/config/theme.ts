// src/config/theme.ts
export const colors = {
  // Primary brand color used across the site
  primary: "#2f5a84",
  // Accent color for hover states, buttons, etc.
  accent: "#4BEAFF",
  // Darker shade for hover or active states
  darkAccent: "#1a3855",
  // Secondary brand color (used in links, highlights)
  secondary: "#1a3855",
  // Light background color
  lightBg: "#f9fafb",
  // Dark background for footers, etc.
  darkBg: "#1a1a1a",
};

export type ThemeColors = typeof colors;
